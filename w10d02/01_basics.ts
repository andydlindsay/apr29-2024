let username: string | number | boolean = 'alice';
username = 'bob';

username = 42;
username = true;

const myNums: number[] = [];

myNums.push(4);
const result = myNums.pop();
// myNums.push('hello');
